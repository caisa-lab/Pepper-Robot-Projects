dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://qisdk.softbankrobotics.com/sdk/maven'
            allowInsecureProtocol = true
        }
    }
}
rootProject.name = "NLP Quiz"
include ':app'
